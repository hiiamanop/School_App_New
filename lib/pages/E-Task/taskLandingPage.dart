import 'package:flutter/material.dart';

class Tasklandingpage extends StatelessWidget {
  const Tasklandingpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('Belum Ada Latihan Yang Tersedia'),),
      ),
    );
  }
}